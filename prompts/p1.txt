this is the code for the signup stuff. i want u to create a prompt for signup management.
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resolvia - Signup</title>
    
    <link rel="preload" as="image" href="assets/building.jpg">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --nutech-green: rgba(8, 81, 68, 0.94);
            --nutech-solid: #085144;
            --text-white: #ffffff;
            --fallback-bg: #042a23; /* Dark green fallback */
            --label-color: #085144; 
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body, html {
            height: 100%;
            width: 100%;
            background: var(--fallback-bg) url('assets/building.jpg') no-repeat center center/cover;
            background-attachment: fixed;
            overflow-x: hidden; 
        }

        .main-container {
            display: flex;
            min-height: 100vh;
            width: 100%;
        }

        /* --- Panels --- */
        .form-panel {
            flex: 1; 
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            z-index: 2; 
        }

        .info-panel {
            position: relative;
            width: 60%; 
            color: var(--text-white);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 1;
            text-align: center;
            padding: 20px;
        }

        /* --- Wave Animation --- */
        .wave-image {
            position: absolute;
            top: 0;
            left: 0; 
            height: 100%;
            width: auto;
            min-width: 100%; 
            transform: translateX(10%); 
            object-fit: cover; 
            pointer-events: none;
            z-index: 0; 
            animation: waveInFromRight 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards;
        }

        @keyframes waveInFromRight {
            from { transform: translateX(150%); }
            to { transform: translateX(10%); }
        }

        @keyframes waveOutRight {
            from { transform: translateX(10%); }
            to { transform: translateX(150%); }
        }

        .wave-exit-right {
            animation: waveOutRight 0.7s cubic-bezier(0.4, 0, 0.2, 1) forwards !important;
        }

        /* --- Glass Card --- */
        .glass-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid rgba(255, 255, 255, 0.25);
            padding: clamp(20px, 5vw, 40px);
            border-radius: 24px;
            width: 100%;
            max-width: 450px;
            color: white;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            max-height: 90vh;
            overflow-y: auto;
            transition: 0.5s ease;
        }

        .card-fade-out {
            opacity: 0;
            transform: scale(0.95);
        }

        /* --- Branding --- */
        .content-wrapper {
            max-width: 400px;
            position: relative;
            z-index: 5;
        }

        .logo-circle {
            width: clamp(120px, 15vw, 180px);
            height: clamp(120px, 15vw, 180px);
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid rgba(255, 255, 255, 0.9);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.25);
            animation: floatLogo 6s ease-in-out infinite;
            margin-bottom: 25px;
        }

        .info-panel h1 {
            font-size: clamp(2rem, 5vw, 3rem);
            font-weight: 700;
            margin-bottom: 10px;
        }

        @keyframes floatLogo {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        /* --- Floating Labels --- */
        .input-group {
            position: relative;
            margin-bottom: 15px;
        }

        .input-group i {
            position: absolute;
            left: 15px;
            top: 28px;
            transform: translateY(-50%);
            color: #444;
            font-size: 0.9rem;
            z-index: 2;
            transition: 0.3s;
        }

        .input-group input {
            width: 100%;
            padding: 22px 15px 8px 45px;
            border-radius: 12px;
            border: 1px solid transparent;
            background: rgba(255, 255, 255, 0.85);
            font-size: 0.95rem;
            color: #222;
            outline: none;
            transition: 0.3s;
        }

        .input-group label {
            position: absolute;
            left: 45px;
            top: 18px;
            color: #666;
            pointer-events: none;
            transition: 0.3s ease all;
            font-size: 0.95rem;
        }

        .input-group input:focus + label,
        .input-group input:not(:placeholder-shown) + label {
            top: 6px;
            left: 15px;
            font-size: 0.7rem;
            color: var(--label-color);
            font-weight: 600;
        }

        .input-group input:focus {
            border-color: var(--label-color);
            background: white;
        }

        .input-group input:focus ~ i {
            color: var(--label-color);
        }

        /* --- Buttons --- */
        .btn-primary, .btn-secondary {
            width: 100%;
            padding: 14px;
            border-radius: 12px;
            border: none;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: 0.3s;
        }

        .btn-primary {
            background-color: var(--nutech-solid);
            color: white;
            margin-top: 5px;
            margin-bottom: 15px;
        }

        .btn-secondary {
            background: white;
            color: var(--nutech-solid);
        }

        /* --- RESPONSIVENESS --- */

        @media (max-width: 1024px) {
            .info-panel { width: 50%; }
        }

        @media (max-width: 900px) {
            .main-container { 
                flex-direction: column; 
                overflow-y: auto; 
            }
            
            .info-panel { 
                width: 100%; 
                height: auto; 
                padding: 60px 20px 40px 20px; 
                order: -1; 
                background-color: var(--nutech-green);
            }
            
            .wave-image { 
                display: none; 
            }
            
            .form-panel { 
                width: 100%; 
                padding: 20px; 
            }
            
            .glass-card { 
                max-width: 100%;
                max-height: none; 
                overflow-y: visible; 
                backdrop-filter: blur(15px);
            }
        }

        @media (max-width: 480px) {
            .glass-card {
                padding: 25px 15px;
            }
            
            .input-group input {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="main-container">
        <div class="form-panel">
            <div class="glass-card" id="signupCard">
                <form action="#" class="sign-up-form">
                    <h2 style="font-size: 2rem; margin-bottom: 30px; text-align: center; color: var(--nutech-solid);">Signup</h2>
                    
                    <div class="input-group">
                        <input type="text" id="userID" placeholder=" " required />
                        <label for="userID">User ID</label>
                        <i class="fas fa-address-card"></i>
                    </div>
                    
                    <div class="input-group">
                        <input type="text" id="fullName" placeholder=" " required />
                        <label for="fullName">Full Name</label>
                        <i class="fas fa-user"></i>
                    </div>
                    
                    <div class="input-group">
                        <input type="email" id="email" placeholder=" " required />
                        <label for="email">Email Address</label>
                        <i class="fas fa-envelope"></i>
                    </div>
                    
                    <input type="button" value="Send OTP" class="btn-otp" style="width: 100%; padding: 12px; border-radius: 12px; border: 1px solid white; background-color: rgba(255,255,255,0.1); color: white; font-weight: 600; font-size: 0.9rem; cursor: pointer; margin-bottom: 15px; transition: 0.3s;" />
                    
                    <div class="input-group">
                        <input type="text" id="otpCode" placeholder=" " />
                        <label for="otpCode">Email Confirmation Code</label>
                        <i class="fas fa-key"></i>
                    </div>
                    
                    <div class="input-group">
                        <input type="password" class="password-field" id="pass" placeholder=" " required />
                        <label for="pass">Password</label>
                        <i class="fas fa-lock"></i>
                    </div>
                    
                    <div class="input-group">
                        <input type="password" class="password-field" id="confirmPass" placeholder=" " required />
                        <label for="confirmPass">Confirm Password</label>
                        <i class="fas fa-lock"></i>
                    </div>

                    <label style="display: flex; align-items: center; gap: 8px; margin-bottom: 15px; font-size: 0.9rem; cursor: pointer; color: white;">
                        <input type="checkbox" id="showPassword" style="accent-color: var(--nutech-solid);"> Show All Passwords
                    </label>
                    
                    <input type="submit" value="Signup" class="btn-primary" />
                    
                    <div style="margin: 15px 0; display: flex; align-items: center; gap: 10px; color: #ccc;">
                        <div style="flex: 1; height: 1px; background: rgba(255,255,255,0.3);"></div>
                        <span>or</span>
                        <div style="flex: 1; height: 1px; background: rgba(255,255,255,0.3);"></div>
                    </div>
                    
                    <a href="login.html" id="toLogin" style="text-decoration: none;">
                        <button type="button" class="btn-secondary" id="sign-in-btn">Login</button>
                    </a>
                </form>
            </div>
        </div>

        <div class="info-panel">
            <img src="assets/wave2.svg" alt="" class="wave-image" id="wave">
            <div class="content-wrapper">
                <img src="assets/logo.png" alt="Logo" class="logo-circle" />
                <h1>Resolvia</h1>
                <h2>Complaint Management System</h2>
            </div>
        </div>
    </div>

    <script>
        // Password Toggle
        document.getElementById('showPassword').addEventListener('change', function() {
            const passwordFields = document.querySelectorAll('.password-field');
            passwordFields.forEach(field => { field.type = this.checked ? 'text' : 'password'; });
        });

        // Page Transition
        document.getElementById('toLogin').addEventListener('click', function(e) {
            e.preventDefault();
            const target = this.href;
            const wave = document.getElementById('wave');
            
            if (wave && window.innerWidth > 900) {
                wave.classList.add('wave-exit-right');
            }
            
            document.getElementById('signupCard').classList.add('card-fade-out');
            
            setTimeout(() => { 
                window.location.href = target; 
            }, 500); 
        });
    </script>
</body>

</html>

also telling the LLM to use express.js apis to work with it.
and use the tables below for otp stuff and email stuff and tell if the attempts for 1 email is 5 then send message that max number of attempts per day for this email etc.

do not tell the LLM each and every minor detail because the LLM is smarter and have better reasoning.
CREATE TABLE Customer (
    customer_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(150) NOT NULL UNIQUE,
    phone_number VARCHAR(20),
    password_hash VARCHAR(255) NOT NULL,
    picture VARCHAR(255),                 -- Path to picture: assets/customers/filename.ext
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        ON UPDATE CURRENT_TIMESTAMP
);
CREATE TABLE email_otp (
    otp_id INT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(150) NOT NULL,
    otp_hash VARCHAR(255) NOT NULL,
    purpose ENUM('signup', 'email_change', 'password_reset') NOT NULL,

    attempts_today TINYINT DEFAULT 0,
    last_sent_at DATETIME NOT NULL,

    expires_at DATETIME NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    INDEX idx_email (email),
    INDEX idx_expires (expires_at)
);
